<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:paddingRight="20dp"
    android:paddingLeft="20dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:background="@drawable/gradient_royal"
    android:layout_height="match_parent">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="40sp"
        android:paddingBottom="5dp"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:id="@+id/user_name_text_view"
        android:text="Dorothy Asks"/>


        <ScrollView
            android:layout_weight="1"
            android:layout_width="match_parent"
            android:paddingTop="10dp"
            android:paddingLeft="5dp"
            android:paddingRight="5dp"
            android:layout_gravity="center_vertical"
            android:id="@+id/question_view_scroll_view"
            android:background="@drawable/gradient_cherry"
            android:layout_height="match_parent">
            <TextView
                android:layout_width="match_parent"
                android:textColor="@color/white"
                android:textSize="36sp"
                android:id="@+id/question_text_view"
                android:layout_height="wrap_content"
                android:text="Share a personal problem and ask your partner’s advice
on how he or she might handle it. Also, ask your partner to
reflect back to you how you seem to be feeling about the
problem you have chosen."/>
        </ScrollView>

    <TextView
        android:gravity="center_horizontal"
        android:textSize="20sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white"

        android:text="Award mark from 0 to 10"/>

    <EditText
        android:layout_width="match_parent"
        android:inputType="number"
        android:maxLength="2"
        android:textColor="@drawable/gradient_cherry"
        android:id="@+id/percentage_edit_text"
        android:layout_height="wrap_content" />

    <Button
        android:layout_gravity="center_horizontal"
        android:layout_width="wrap_content"
        android:textColor="@color/white"
        android:id="@+id/award_button"
        android:layout_height="wrap_content"
        android:background="@drawable/gradient_cherry"
        android:text="Award"/>
</LinearLayout>